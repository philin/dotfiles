A way to never leave home

## IDEs

### PyCharm
if you aren't using pro then sync isn't available

`Export Settings` and then import on the other computer

### Visual Studio Code (VS Code)
settings sync doesn't seem to work
